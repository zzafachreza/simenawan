import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export default function () {
    return (
        <View>
            <Text>Menu Pendidikan Edit</Text>
        </View>
    );
}

const styles = StyleSheet.create({});
